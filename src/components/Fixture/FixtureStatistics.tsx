interface FixtureStatisticsProps {
  homeId: string;
  awayId: string;
}
const FixtureStatistics = ({ homeId, awayId }: FixtureStatisticsProps) => {
  // separar la respone en home y away
  // mapear sobre las listas y mostrar los resultados
  return <section>{}</section>;
};

export default FixtureStatistics;
